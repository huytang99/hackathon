# Station B works here

This directory belongs to Station B (the strongest dev) for the whole contest.
Station A never edits anything in here, and you never edit anything outside it.
That single rule is what keeps two Copilot sessions from colliding.

Start by copying the closest archetype from `app/(core)/` into here and renaming it:

    cp -r "app/(core)/requests" "app/(feature)/<your-feature>"

Then ask Station A for the nav link. Do not add it yourself.
