# Feature components live here

One subfolder per feature: `components/feature/<feature>/`, owned by whoever
owns that feature folder in `PLAN.md`.

Compose from `components/ui/**` (22 shadcn components). Never edit those, and
never hand-roll a button, input, modal, dropdown or table.
